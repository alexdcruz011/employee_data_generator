# 🧑‍💼 Employee Data Generator

A cross-platform PySide6 widget that generates synthetic employee records and exports them to Excel — perfect for testing, demos, or portfolio datasets.

## 🚀 Features

- Input number of records to generate
- Select folder to save output
- Generate realistic employee data:
  - Employee ID
  - Full name (randomized)
  - Department
  - Salary range
  - Hire date (randomized between Jan 2020 and today)
- Export to Excel (`.xlsx`) using `pandas`
- Timestamp display for data generation

## 🛠 Requirements

- Python 3.11+
- Recommended: Windows or Linux (WSLg-compatible)

## 📦 Installation

```bash
pip install -r requirements.txt